<h1>All registered volunteers</h1>
<p>Click volunteer name to view volunteer and their current project</p>


<% if @volunteers.any? %>
  <ul>
    <% @volunteers.each do |volunteer| %>
      <li><a href="/volunteers/<%= volunteer.id %>"><%= volunteer.name %></a></li>
    <% end %>
  </ul>
<% else %>
  <p>We don't currently have any registered volunteers</p>
<% end %>

<h2><a href="/volunteers/new">Add a new volunteer</a></h2>

<h3><a href="/projects">Return to list of projects</h3>
